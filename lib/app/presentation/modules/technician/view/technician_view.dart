import 'package:flutter/material.dart';

class TechnicianView extends StatelessWidget {
  const TechnicianView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Tecnico View'));
  }
}
